# Example environment variables for the Financial Report Analyzer.
# Copy this file to `.env` (which is gitignored) and fill in real values.
# Do NOT commit real secrets.

# Optional: only needed for the hybrid LLM engine
# (financial_signal_engine_LLMv1.py). Leave blank if you only use the
# default V2 lexicon engine.
ANTHROPIC_API_KEY=
