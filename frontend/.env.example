# Optional. Leave unset in local dev: Vite proxies /api → http://127.0.0.1:8000
# (avoids CORS). Use this when the UI is *not* served by `npm run dev` (e.g.
# a built bundle or another host) and you call the API by full URL.
# VITE_API_URL=http://127.0.0.1:8000
