# CS329 Financial Report Analyzer — Data Ingestion
# requirements.txt

requests>=2.31.0
selenium>=4.18.0
pandas>=2.0.0
spacy>=3.7
negspacy>=1.0.4
nltk>=3.8
streamlit>=1.44.0
datasets<3.0.0

torch>=2.0.0
transformers>=4.40.0
scikit-learn>=1.3.0
joblib>=1.3.0
numpy>=1.24.0

# FastAPI bridge (backend/ directory) consumed by the React frontend
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
python-dotenv>=1.0.0

# V3 default API engine (`HybridSignalEngineV3`) imports the hybrid LLM module
# for the optional third tier; `anthropic` must be importable even when
# `use_llm=False` on the V3 class.
# Pure LLM engine + hybrid rescore: also require ANTHROPIC_API_KEY at runtime.
anthropic>=0.34.0